anthropic>=0.49.0          # Claude API for evaluation, tailoring, field matching
django>=5.0                # web dashboard API server
playwright>=1.51.0         # browser automation for auto-apply
pyyaml>=6.0.2              # human-editable config files
sqlite-utils>=3.38         # SQLite schema + query helpers
requests>=2.32.3           # HTTP calls to ATS APIs
markdown>=3.7              # markdown processing for resume rendering
weasyprint>=65.0           # markdown → PDF via HTML/CSS
python-dotenv>=1.1.0       # .env loading (ANTHROPIC_API_KEY etc.)
pydantic>=2.11.0           # structured validation of LLM responses and config
pytest>=8.3.5              # test runner
ruff>=0.11.0               # linting and formatting
