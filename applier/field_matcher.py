_UNKNOWN = "UNKNOWN"


def match(label: str, field_answers: dict) -> str:
    """Return the answer for a form field label, or UNKNOWN if unresolvable."""
    # Phase 4 — fuzzy + Claude-assisted matching
    raise NotImplementedError
